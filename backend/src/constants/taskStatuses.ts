export const TASK_STATUSES = {
    NOT_SET: 'not set',
    PENDING: 'pending',
    ON_HOLD: 'on-hold',
    COMPLETED: 'completed',
};

export const TASK_STATUS_VALUES:string[] = Object.values(TASK_STATUSES);
